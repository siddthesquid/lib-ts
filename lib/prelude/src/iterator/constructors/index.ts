import { constant, single } from "./constant"
import { create } from "./create"
import { empty } from "./empty"
import { from } from "./from"
import { generate } from "./generate"
import { to, until } from "./to"
import { unit } from "./unit"

const Constructors = {
  create,
  generate,
  constant,
  single,
  empty,
  to,
  until,
  from,
  unit,
}

export { Constructors }
