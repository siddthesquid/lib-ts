import { as } from "./as"
import { closure } from "./closure"
import { declare } from "./declare"
import { id } from "./id"
import { sync } from "./sync"

const Constructors = {
  as,
  id,
  sync,
  declare,
  closure,
}

export { Constructors }
